const API_BASE_URL = 'https://cctv-backend-six.vercel.app/api';

export const API_ENDPOINTS = {
  LOGIN: `${API_BASE_URL}/auth/login`,
  CAMERAS: `${API_BASE_URL}/cameras`,
};

export default API_BASE_URL;
